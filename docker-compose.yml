services:
  usuario-db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: usuario_user
      POSTGRES_PASSWORD: usuario_secret
      POSTGRES_DB: usuario_db
    volumes:
      - usuario_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U usuario_user -d usuario_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  package-db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: package_user
      POSTGRES_PASSWORD: package_secret
      POSTGRES_DB: package_db
    volumes:
      - package_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U package_user -d package_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  tracking-db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: tracking_user
      POSTGRES_PASSWORD: tracking_secret
      POSTGRES_DB: tracking_db
    volumes:
      - tracking_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tracking_user -d tracking_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data

  usuario-service:
    build:
      context: ./services/usuario-service
      dockerfile: Dockerfile
    environment:
      DB_HOST: usuario-db
      DB_PORT: "5432"
      DB_USER: usuario_user
      DB_PASSWORD: usuario_secret
      DB_NAME: usuario_db
    depends_on:
      usuario-db:
        condition: service_healthy
    ports:
      - "8001:8000"

  package-service:
    build:
      context: ./services/package-service
      dockerfile: Dockerfile
    environment:
      DB_HOST: package-db
      DB_PORT: "5432"
      DB_USER: package_user
      DB_PASSWORD: package_secret
      DB_NAME: package_db
      USUARIO_SERVICE_URL: http://usuario-service:8000
      TRACKING_SERVICE_URL: http://tracking-service:8000
      REDIS_URL: redis://redis:6379/0
    depends_on:
      package-db:
        condition: service_healthy
      usuario-service:
        condition: service_started
      tracking-service:
        condition: service_started
      redis:
        condition: service_started
    ports:
      - "8002:8000"

  tracking-service:
    build:
      context: ./services/tracking-service
      dockerfile: Dockerfile
    environment:
      DB_HOST: tracking-db
      DB_PORT: "5432"
      DB_USER: tracking_user
      DB_PASSWORD: tracking_secret
      DB_NAME: tracking_db
      PACKAGE_SERVICE_URL: http://package-service:8000
      REDIS_URL: redis://redis:6379/0
    depends_on:
      tracking-db:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - "8003:8000"

  notification-service:
    build:
      context: ./services/notification-service
      dockerfile: Dockerfile
    environment:
      REDIS_URL: redis://redis:6379/0
    depends_on:
      redis:
        condition: service_started
    ports:
      - "8004:8000"

  api-gateway:
    build:
      context: ./gateway
      dockerfile: Dockerfile
    ports:
      - "8000:80"
    depends_on:
      - usuario-service
      - package-service
      - tracking-service

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - api-gateway

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.15.0
    environment:
      DATA_SOURCE_NAME: postgresql://tracking_user:tracking_secret@tracking-db:5432/tracking_db?sslmode=disable
    depends_on:
      tracking-db:
        condition: service_healthy

  prometheus:
    image: prom/prometheus:v2.52.0
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    depends_on:
      - usuario-service
      - package-service
      - tracking-service
      - notification-service

  grafana:
    image: grafana/grafana:11.0.0
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    depends_on:
      - prometheus

volumes:
  usuario_db_data:
  package_db_data:
  tracking_db_data:
  redis_data:
  grafana_data:
